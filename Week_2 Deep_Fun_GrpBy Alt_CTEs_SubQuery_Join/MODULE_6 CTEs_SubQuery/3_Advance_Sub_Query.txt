1. Second higest salary কত ? 
2.  কোন কোন employee তাদের manager এর চেয়ে বেশি salary পায়?
3 . কোন কোন employee তাদের manager এর মতো job করে



SELECT *
FROM EMPLOYEES;

-- 1ST NORMAL WAY -- 
SELECT DISTINCT SALARY
FROM EMPLOYEES
ORDER BY SALARY DESC
LIMIT 1
OFFSET 1;

SELECT *
FROM EMPLOYEES
WHERE SALARY = ( SELECT DISTINCT SALARY
				FROM EMPLOYEES
				ORDER BY SALARY DESC
				LIMIT 1
				OFFSET 1
                );

 -- SECOND HIGETS SELARY INFO / STANDARD WAY --
 
SELECT MAX(SALARY)
 FROM EMPLOYEES
 WHERE SALARY < (SELECT MAX(SALARY)
				FROM EMPLOYEES
                ); 
 SELECT *
 FROM EMPLOYEES
 WHERE SALARY = (SELECT MAX(SALARY)
				 FROM EMPLOYEES
				 WHERE SALARY < (SELECT MAX(SALARY)
								FROM EMPLOYEES
								) 
				);
 



-- -------- WHOS EMPLOYESS IS HIGESET SALARAY THEN HIS MANAGER SALARY? ---------------------- --


SELECT *
FROM EMPLOYEES AS EMP
WHERE SALARY > (SELECT SALARY
				FROM EMPLOYEES AS MGR
                WHERE EMP.MANAGER_ID = MGR.EMPLOYEE_ID
                );
				
SELECT *FROM EMPLOYEES;

SELECT MANAGER_ID,SALARY
FROM EMPLOYEES
ORDER BY SALARY DESC;

SELECT 
	MANAGER_ID , 
    GROUP_CONCAT(EMPLOYEE_ID) AS FOLLOWER
FROM EMPLOYEES
GROUP BY MANAGER_ID; 


SELECT EMPLOYEE_ID ,SALARY
FROM EMPLOYEES
WHERE EMPLOYEE_ID IN (168,174,148,149);





-- WHOS EMPLOYES SAME TITLE JOB ALSO HIS MANAGER JOB TITLE ? --
SELECT *
FROM EMPLOYEES; 


SELECT *
FROM EMPLOYEES AS EMP
WHERE DEPARTMENT_ID = (
						SELECT DEPARTMENT_ID
                        FROM EMPLOYEES AS MNG
                        WHERE EMP.MANAGER_ID = MNG.EMPLOYEE_ID
						);
                        
SELECT *
FROM EMPLOYEES AS EMP
WHERE JOB_ID = (
				SELECT JOB_ID
                FROM EMPLOYEES AS MNG
                WHERE EMP.MANAGER_ID = MNG.EMPLOYEE_ID
                );
